restructure project to put code/scene/asset into same folder
move variable setters to custom funcs

add sounds

world item damp upwards movement, only after bounce

track all spawned items globally, and check all if inside despawned chunk and delete 

tilemap foreground layer (for water, add water tile to foreground layer in addition to bg layer)

display interaction label over interaction object? make component 2d for positional hint

throw dynamite

hand gun

basePlayer class and extend with default character "Classico"

character selection screen

health bar ( auto regenerate ), fall damage
