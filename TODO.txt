pull world items towards player

block break particles

tilemap foreground layer (for water, add water tile to foreground layer in addition to bg layer)

throw dynamite

hand gun

basePlayer class and extend with default character "Classico"

character selection screen

terrain generator ( per block: noise, threshold, seed offset, height range, priority )
	virtual air block for caves (is_air= true)
	chunks, not threaded
	create_empty player spawn area


furnace ( multi-block sprite with interaction component )

bat ( Creature, neutral )

health bar ( auto regenerate )
